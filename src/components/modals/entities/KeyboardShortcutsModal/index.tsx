import { Dialog, DialogContent } from "@/components/ui/Dialog";
import React from "react";

type Props = {};

const KeyboardShortcutsModal = (props: Props) => {
  return (
    <Dialog open={false}>
      <DialogContent>asas</DialogContent>
    </Dialog>
  );
};

export default KeyboardShortcutsModal;
