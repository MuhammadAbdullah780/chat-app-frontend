import { Dialog, DialogContent } from "@/components/ui/Dialog";
import React from "react";

const CreateNewGroupModal: React.FC = () => {
  return (
    <Dialog open={false}>
      <DialogContent>asas</DialogContent>
    </Dialog>
  );
};

export default CreateNewGroupModal;
