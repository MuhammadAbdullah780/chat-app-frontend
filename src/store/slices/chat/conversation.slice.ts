import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const chatConversationSlice = createSlice({
  name: "individual_chat_conversation",
  initialState,
  reducers: {},
});

export const {} = chatConversationSlice.actions;
export default chatConversationSlice.reducer;
