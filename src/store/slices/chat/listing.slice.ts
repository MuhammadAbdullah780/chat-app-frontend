import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const chatListingSlice = createSlice({
  name: "chat_listing",
  initialState,
  reducers: {},
});

export const {} = chatListingSlice.actions;
export default chatListingSlice.reducer;
